#include <unistd.h>

/**
* _punchar - writes the character c to stdout
* @c: the characrter to print
*
* Return: On success 1.
* 	Om error, -1 is returned, and errno is set appropriately
*/
int _punchar(char c)
{
return (write(1, &c, 1));
}
